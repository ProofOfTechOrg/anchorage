---
'@proofoftech/fleet-control': patch
---

Validate inventory page structure and resource identity before recording absence, removing ingress or verifying secret revocation. Keep SDK pagination and the direct/Wrangler lookup paths consistent when metadata is incomplete.
