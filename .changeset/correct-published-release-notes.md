---
'@proofoftech/flowsafe': patch
---

Enforce the shared D1 table-prefix syntax and length contract in every public signal, schedule, and background-task storage constructor, subclass, and factory. Correct the FlowSafe 0.13.0 release notes so shipped changes are no longer duplicated under `Unreleased`.
