# do-runner/

## Files

| File | What | When to read |
| ---- | ---- | ------------ |
| `init.ts` | `init(env)` import-swap — builds D1-backed Mastra storage from the `DB` binding (or an injected `storage`), returns swapped `createWorkflow`/`createStep`/`runtime` factories, threads `requestContextForRun`, and carries the host DO's one pubsub identity (`InitOptions.pubsub`, an instance — `createHostPubSub()` for the default — echoed on `InitResult.pubsub` so every consumer takes THAT one; absent ⇒ undefined and the host is byte-identical); `DORunnerEnv`/`InitOptions`/`InitResult`/`InitSource` | Wiring workflow definitions into the runner, changing how storage, pubsub, and factories are built |
| `runtime.ts` | `RunnerRuntime` — register/start/resume/status/workflowIds, per-run FIFO lock, `PATH_SAFE_ID_PATTERN` validation (runId REQUIRED at `start()` — no generation fallback, INV-1; workflowId at `register()`), error taxonomy, the `requestContextForRun` provider seam (`RunLeg` with resumed step + `suspendedAt` + `resumeCount`; every leg mints the breakwater workflow-scope key AND — for tenant-salted runIds — the opaque isolation-scope key, provider values merge over both), the pluggable `ResumeLedger` (default in-memory; the DO shell adopts a durable one) feeding `RunSummary.resumeCount`, the host `pubsub` identity (`RunnerRuntimeOptions.pubsub` → `get pubsub`; threaded by init() and passed to both `createRun({ runId, pubsub })` sites since Track A, CI-M-002-002 — undefined ⇒ core defaults a fresh emitter, byte-identical), widened `status()` projection | Changing run lifecycle, id validation, locking, error mapping, or the requestContext seam |
| `durable-object.ts` | `DurableObjectRunner` abstract DO — `fetch()` HTTP routing (`POST /runs` requires the trusted Worker's runId; every route asserts path ≡ `ctx.id.name`, INV-1), the `protected get tenantId` recovering the tenant from the DO's own idFromName identity (throws when absent — an unscoped grant store is a cross-tenant mint), the `ctx.storage`-backed resume-ledger adoption, and the per-run WebSocket stream route `GET /runs/:wf/:runId/stream` (workerd-only, guarded by `acceptWebSocket` presence → 426 fallback; broadcasts the authoritative `RunSummary` at each start/resume boundary + an on-connect snapshot, DL-018; per-socket isolated fan-out) | Adding or changing DO HTTP routes, tenant recovery, the run stream, or error responses |
| `thread-do.ts` | `ThreadDurableObject` abstract per-thread DO (DL-002/DL-020) — the host for a thread's agent loop, addressed `idFromName(threadId)` where the threadId is tenant-minted, so `id.name` carries the tenant exactly like a runId. Template-method `fetch()`: it asserts the request's AUTHENTICATED tenant (`THREAD_TENANT_HEADER`, `x-flowsafe-tenant`) against the name's prefix and only then dispatches to the subclass's `route(request, scope)` — so a track adding a route cannot forget the check. The header is load-bearing: a name-vs-path check alone cannot catch tenant B presenting a valid token for tenant A's threadId (name and path agree there). Its MINTER is host-kit's `createThreadTopology` — mint and verify ship together (stream-ticket's posture), because the house DO-forwarding idiom (`hub-topology`'s `stub.fetch(request)`) forwards the CLIENT's headers verbatim: reach a thread DO through the topology, never the raw namespace. `build()` supplies the init() wiring, memoized per instance (one pubsub identity) with the durable resume ledger adopted like `DurableObjectRunner`. Fail-closed: `ThreadIdentityError` (carries `status = 403`) on a missing/foreign tenant or a name carrying no INV-3 prefix; every other error rides the SHARED `doErrorResponse` taxonomy, so a run driven through `scope.init.runtime` keeps its 404/409/400 instead of collapsing to 500. Node-loadable (classic ctor+fetch, no `cloudflare:workers` import) | Building the agent-loop or signal routes on a thread DO (Tracks A/C), changing thread identity |
| `do-error-response.ts` | `doErrorResponse(error)` — the ONE error→HTTP mapping both DO shells answer with (unknown run/workflow ⇒ 404, not-suspended/already-exists ⇒ 409, invalid request ⇒ 400, else 500; a shell's own error states its status via a `status` field, as `ThreadIdentityError` does for 403). Its own leaf so neither DO base depends on the other — the Worker-side half of the contract is host-kit's `RunRouteError`/`doSummary`, which pass these statuses back to the caller | Adding a DO error type or changing a status |
| `pubsub.ts` | The host DO's ONE pubsub identity (DL-001): the `HostPubSub` seam type and `createHostPubSub()` — core's in-process `EventEmitterPubSub`, which is right inside a DO, where idFromName already serializes publisher and subscriber into one isolate. Exists because core creates a FRESH emitter per `createRun()` when none is passed, so two defaulting call sites publish to different feeds and `observe()` replay silently sees nothing; a host passes ONE instance to `init()` (`InitOptions.pubsub`) and every consumer takes it back off `InitResult.pubsub` (the thread DO inherits it through its memoized `InitResult`) rather than building its own. `init()` also threads it into `RunnerRuntimeOptions.pubsub` (alongside storage/ledger), so a host reaches the runtime with NO host change — every DO subclass already returns init()'s runtime from `build()`, which returns a `RunnerRuntime`, not an `InitResult`. **Track 0 ships the identity + the seam only — nothing hands it to core yet**: the runtime HOLDS it (`get pubsub`) but its two `createRun` sites still let core default their emitter, and Track A passes it in (CI-M-002-002). Opt-in: absent ⇒ undefined everywhere ⇒ polling fallback, byte-identical | Threading pubsub into createRun (Track A), streaming agent events, changing the pubsub seam |
| `hub-do.ts` | `HubDurableObject` abstract per-tenant hub DO — the fan-out channel for tenant-wide approval stream events + reviewer presence (run progress never flows here, DL-009). `POST /internal/event` (trusted host forwards each event; asserts `event.record.tenantId` ≡ `id.name`) fans out to the hibernatable subscribers; `GET /subscribe` accepts a socket, records presence in its attachment, and broadcasts the roster. `get tenantId` reads the bare `id.name` (no ':' join, fail-closed). Node-loadable (WebSocketPair via globalThis, no `cloudflare:workers` import); WS path proven by the spike. Uses a local structural `HubStreamEvent` — never imports approval-api (acyclic) | Implementing or changing the hub fan-out, presence, or a host's `ShowcaseHub`/`FlowsafeHub`/`DemoHub` subclass |
| `cf-types.ts` | Structural subsets of the Cloudflare runtime types (`D1DatabaseBinding`, `WebSocketLike`/`WebSocketPairLike`, the DO state shapes widened with the OPTIONAL Hibernatable-WebSocket API), the shared `newWebSocketPair()`/`safeSend()` WS helpers, and the `AssertTrue<DurableObjectState extends …>` compile pins (a REQUIRED-shape pin catches a `@cloudflare/workers-types` drift). Type-only workers-types import — never reaches the emitted `.d.ts` | Widening the DO/WS structural surface or the drift pins |
| `hub-do.test.ts` | `HubDurableObject` fan-out / tenant-mismatch / fail-closed-identity / presence-roster / per-socket-isolation (a throwing socket never starves the others) tests | Changing hub behavior |
| `d1-storage.ts` | `createD1Storage()` — D1-backed Mastra storage wrapper — plus `purgeExpiredWorkflowRuns()` (terminal-only TTL retention purge; optional `artifactStore` deletes each purged run's artifacts BEFORE its row — the row is the only record of their keys, so hosts with R2 must pair it; BOTH paths are `limit`-batched per firing — artifact-paired default 100 against the subrequest budget with per-run failure isolation, row-only default 1000 as one LIMIT-batched DELETE against the D1 per-query budget; the shrinking eligible set is the cursor), `purgeTenant()` (complete offboarding: ANY-status snapshots by the INV-3 range predicate + agent-memory rows — messages by `thread_id` and threads/resources by `id`, all three DELETEs run concurrently via `Promise.all` (child-before-parent order is non-load-bearing: no FK, distinct counter keys, each keeps its own missing-table tolerance), same range over the salted ids, missing tables read as empty — + approvals + Track E's flowsafe-owned `flowsafe_signal_subscriptions` (by `tenant_id`, the approvals leg — counter `subscriptions`, retention `none`, reaped only here) + per-surviving-run artifacts) `purgeExpiredThreads()` (the agent-memory thread TTL, docs/agent-memory-tenancy.md item 7 — keyed on `mastra_threads.updatedAt` because threads are not per-run and have no terminal status; messages go WITH their thread and BEFORE it (a message has `createdAt` but no `updatedAt`, so no per-message idleness signal exists and thread-first would strand them beyond every later firing) — enforced STRUCTURALLY by a `NOT EXISTS` guard, not by statement order, because the writer is not atomic either (saveMessages' insert and its updatedAt bump are independent calls under one Promise.all, so an updatedAt-only guard deletes a thread out from under a message that just landed); `mastra_resources` untouched — the owner's, not the thread's; LIMIT-batched, id lists chunked under D1's 100-bound-param ceiling; opt-in via the worker's `THREAD_RETENTION_DAYS`) `purgeExpiredBackgroundTasks()` (Track B: the `mastra_background_tasks` TTL cleanup — mirrors core `BackgroundTaskManager.cleanup`'s two windows, completed by `completedAt` fast + failed/cancelled/timed_out slow, at the STORAGE layer so a cron reaps without a live manager; missing table → 0; anchored by `BACKGROUND_TASK_TTL_PURGE_TABLES`) `purgeExpiredNotifications()` + `purgeExpiredThreadState()` (Track C: the `mastra_notifications` TTL — terminal rows past `updatedAt`, pending kept — and the `mastra_thread_state` TTL by `updatedAt`, each opt-in via a `*_RETENTION_DAYS` var, anchored by `NOTIFICATION_TTL_PURGE_TABLES` / `THREAD_STATE_TTL_PURGE_TABLES`; the two tables are flowsafe-owned D1 domains composed into `createD1Storage` via the injected `domains`, ranged in `purgeTenant` over their salted `thread_id`, counters `notifications`/`threadState`) and `ensureSnapshotRunIdIndex()` (app-owned `run_id` index; couples to Mastra's column name — guarded by test; DDL memoized per (database, prefix) with clear-on-failure, so the reaper loop pays no per-tenant round-trip). Home of `TENANT_RANGE_PURGE_TABLES` — the EXTENSIBLE tenant-range purge inventory (DL-003): a track adopting a `mastra_*` domain appends one row here plus its `TenantRangePurgeCounter`/`PurgeTenantResult` pair, which the types force in the same change (Track B adopted `mastra_background_tasks`, ranged over its salted `run_id`, counter `backgroundTasks`) (a counter with no result field fails the compile-time pin; a result field with no counter fails purgeTenant's `memory` initializer). All purges read a missing table as empty (Mastra creates them lazily; run-less tenants must still offboard) | Changing the D1 storage adapter, retention, the thread TTL, or tenant offboarding |
| `memory-id.ts` | Agent-memory tenancy chokepoint (docs/agent-memory-tenancy.md): `mintThreadId` (`${tenantId}_${uuid}`), `mintResourceId` (`${tenantId}_${resourceKey}`, key PATH_SAFE-validated), `tenantOfMemoryId` (delegates to `tenantOfRunId` — ONE decode), `tenantOwnsMemoryId` (exact prefix check); both mints re-refuse non-INV-3 + reserved tenants. The ONLY constructors for memory ids — hosts never accept a client-supplied threadId/resourceId | Building the agents-with-memory feature, changing memory id shape |
| `memory-id.test.ts` | Mint shape + uuid seam, INV-3/reserved refusals, PATH_SAFE key refusals, `_`-in-key boundary exactness, decode delegation pin (byte-identical with `tenantOfRunId`), acme-vs-acmecorp ownership pins | Changing memory-id behavior |
| `memory-recall-tenancy.test.ts` | The recall-path proof (docs/agent-memory-tenancy.md item 6): core's own `MastraMemory` impl (`MockMemory`) over the REAL D1Store, two tenants keyed by the SAME business key `'user-1'`, pinning all three recall surfaces an agent turn uses — `recall()`, `listThreads({filter:{resourceId}})`, resource-scoped `getWorkingMemory()` — plus the foreign-thread defense-in-depth case. The schema guard proves the ROWS are disjoint; this proves the API an agent reads through is what scopes them | Changing memory ids, the recall seam, or a @mastra/core memory bump |
| `thread-do.test.ts` | Thread-DO identity (the VERIFY half; host-kit/thread-topology.test.ts owns the mint half + their round-trip): the C-S4 cross-tenant case (a valid token for another tenant's thread), a missing tenant header, the acme-vs-acmecorp boundary, a name with no INV-3 prefix, no `id.name`, build-once memoization, and no wiring built for a refused request | Changing thread identity or the route dispatch |
| `breakwater-keys.ts` | The four `BREAKWATER_*` requestContext key literals (approved-connectors, actor, workflow-scope, isolation-scope), mirrored by value — literal equality with breakwater enforced by the e2e tripwires | Changing a cross-package requestContext key |
| `resume-ledger.ts` | `ResumeLedger` seam + `InMemoryResumeLedger` + `DurableStorageResumeLedger` (ctx.storage-backed; stores `[stepKey, count]` pairs; `increment` takes optional `prior` counts as a read-elision hint — the runtime passes what it read under the same run lock, so a resume costs ONE billed get, and `status()` reads the ledger only for suspended runs). A lost ledger is fail-closed for grants but an AVAILABILITY bug (approved action silently no-ops), so the DO shell adopts the durable one | Changing resume-ordinal durability |
| `path-safe-id.ts` | `PATH_SAFE_ID_PATTERN` (runId/workflowId), `TENANT_ID_PATTERN` (INV-3 — the charset that makes the runId tenant prefix and the range purge EXACT; never conflate the two), `RESERVED_TENANT_IDS` (`system`, the TCB's audit identity — lives in this leaf so approval-api's resolver and host-kit's verifier both enforce it without a layering inversion; host-kit re-exports it), `tenantOfRunId()` — the ONE place INV-1's `${tenantId}_${uuid}` carrier is decoded (four hand-rolled `indexOf('_')` copies drifted; one skipped validation), `tenantOwnsSaltedId(tenantId, id)` — the ONE tenant-salted ownership predicate (runIds and memory ids alike; exact because INV-3 excludes `_`/backtick), and `assertMintableTenantId`/`mintSaltedId` — the ONE mint-path precondition + constructor (INV-3 + reserved refusal, generic-`Error` programmer-error contract, deliberately NOT `TenantResolutionError`) shared by every salted-id mint | Changing an id charset, the reserved-identity list, the tenant decode, the ownership predicate, or the mint precondition |
| `index.ts` | Subpackage barrel — public do-runner API and types | Finding the do-runner export surface |
| `runtime.test.ts` | `RunnerRuntime` lifecycle / lock / validation / scope-minting tests | Adding runtime tests, debugging lifecycle behavior |
| `durable-object.test.ts` | `DurableObjectRunner` HTTP-routing + suspend/resume tests | Adding DO tests, debugging routing or resume |
| `d1-storage.test.ts` | `purgeExpiredWorkflowRuns` + `purgeExpiredThreads` + `purgeTenant` against real SQLite: terminal-vs-live statuses, TTL boundary, prefix-neighbor range exactness ('abc' vs 'abcdefg'), full offboarding, missing-table-as-empty (run-less tenant, fresh deployment, absent memory tables), retention↔artifact pairing incl. artifacts-first crash ordering, the thread TTL's updatedAt-not-createdAt keying + messages-before-thread crash ordering + D1 bind-chunk ceiling + resources-untouched pins, and `purgeTenant`'s two non-atomicity pins (a run landing in the SELECT→DELETE window is reaped with its artifacts unenumerated; a failing approvals DELETE leaves the snapshots already gone) — both reachable only by self-service reset | Adding retention/offboarding tests |
| `mastra-schema-guard.test.ts` | The REAL `@mastra/cloudflare-d1` D1Store over node:sqlite: the `MASTRA_TABLES` inventory — a STRUCTURE, not a name list (DL-003): each entry declares that table's OFFBOARDING purge coverage (`run-id-range`/`tenant-range`/`unadopted`) AND its RETENTION story (`run-ttl`/`thread-ttl`/`cascade`/`none` — where `none` demands a `because`, so "we decided" and "nobody thought about it" stop being indistinguishable), covering all three DL-003 legs: an undeclared table fails, a `tenant-range` declaration not wired into `TENANT_RANGE_PURGE_TABLES` fails, a retention kind naming a purge that does not target that table fails, and a `cascade` onto a parent that expires nothing fails — plus the `run_id` + memory-table column-name guards (the purge range predicates and the thread TTL's `updatedAt` ride them), the two-tenant same-business-key adversarial case (disjoint salted rows; purge reaps exactly one tenant), and the purge-vs-resume race regression | A `@mastra/core` bump, adopting a `mastra_*` domain, touching purge semantics, or the memory tenancy chokepoints |
| `path-safe-id.test.ts` | Character-exhaustive INV-3 pins: no valid tenantId char in `[0x5F,0x60]`; tenant-salted runIds stay PATH_SAFE | Changing either id pattern |
