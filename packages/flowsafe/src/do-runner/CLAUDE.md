# do-runner/

## Files

| File | What | When to read |
| ---- | ---- | ------------ |
| `init.ts` | `init(env)` import-swap — builds D1-backed Mastra storage from the `DB` binding (or an injected `storage`), returns swapped `createWorkflow`/`createStep`/`runtime` factories, threads `requestContextForRun`; `DORunnerEnv`/`InitOptions`/`InitResult`/`InitSource` | Wiring workflow definitions into the runner, changing how storage and factories are built |
| `runtime.ts` | `RunnerRuntime` — register/start/resume/status/workflowIds, per-run FIFO lock, `PATH_SAFE_ID_PATTERN` validation (runId REQUIRED at `start()` — no generation fallback, INV-1; workflowId at `register()`), error taxonomy, the `requestContextForRun` provider seam (`RunLeg` with resumed step + `suspendedAt` + `resumeCount`; every leg mints the breakwater workflow-scope key AND — for tenant-salted runIds — the opaque isolation-scope key, provider values merge over both), the pluggable `ResumeLedger` (default in-memory; the DO shell adopts a durable one) feeding `RunSummary.resumeCount`, widened `status()` projection | Changing run lifecycle, id validation, locking, error mapping, or the requestContext seam |
| `durable-object.ts` | `DurableObjectRunner` abstract DO — `fetch()` HTTP routing (`POST /runs` requires the trusted Worker's runId; every route asserts path ≡ `ctx.id.name`, INV-1), the `protected get tenantId` recovering the tenant from the DO's own idFromName identity (throws when absent — an unscoped grant store is a cross-tenant mint), and the `ctx.storage`-backed resume-ledger adoption | Adding or changing DO HTTP routes, tenant recovery, or error responses |
| `d1-storage.ts` | `createD1Storage()` — D1-backed Mastra storage wrapper — plus `purgeExpiredWorkflowRuns()` (terminal-only TTL retention purge; optional `artifactStore` deletes each purged run's artifacts BEFORE its row — the row is the only record of their keys, so hosts with R2 must pair it; BOTH paths are `limit`-batched per firing — artifact-paired default 100 against the subrequest budget with per-run failure isolation, row-only default 1000 as one LIMIT-batched DELETE against the D1 per-query budget; the shrinking eligible set is the cursor), `purgeTenant()` (complete offboarding: ANY-status snapshots by the INV-3 range predicate + approvals + per-surviving-run artifacts) and `ensureSnapshotRunIdIndex()` (app-owned `run_id` index; couples to Mastra's column name — guarded by test; DDL memoized per (database, prefix) with clear-on-failure, so the reaper loop pays no per-tenant round-trip). Both purges read a missing snapshot table as empty (Mastra creates it lazily with the first run; run-less tenants must still offboard) | Changing the D1 storage adapter, retention, or tenant offboarding |
| `breakwater-keys.ts` | The four `BREAKWATER_*` requestContext key literals (approved-connectors, actor, workflow-scope, isolation-scope), mirrored by value — literal equality with breakwater enforced by the e2e tripwires | Changing a cross-package requestContext key |
| `resume-ledger.ts` | `ResumeLedger` seam + `InMemoryResumeLedger` + `DurableStorageResumeLedger` (ctx.storage-backed; stores `[stepKey, count]` pairs; `increment` takes optional `prior` counts as a read-elision hint — the runtime passes what it read under the same run lock, so a resume costs ONE billed get, and `status()` reads the ledger only for suspended runs). A lost ledger is fail-closed for grants but an AVAILABILITY bug (approved action silently no-ops), so the DO shell adopts the durable one | Changing resume-ordinal durability |
| `path-safe-id.ts` | `PATH_SAFE_ID_PATTERN` (runId/workflowId), `TENANT_ID_PATTERN` (INV-3 — the charset that makes the runId tenant prefix and the range purge EXACT; never conflate the two), `RESERVED_TENANT_IDS` (`system`, the TCB's audit identity — lives in this leaf so approval-api's resolver and host-kit's verifier both enforce it without a layering inversion; host-kit re-exports it), and `tenantOfRunId()` — the ONE place INV-1's `${tenantId}_${uuid}` carrier is decoded (four hand-rolled `indexOf('_')` copies drifted; one skipped validation) | Changing an id charset, the reserved-identity list, or the tenant decode |
| `index.ts` | Subpackage barrel — public do-runner API and types | Finding the do-runner export surface |
| `runtime.test.ts` | `RunnerRuntime` lifecycle / lock / validation / scope-minting tests | Adding runtime tests, debugging lifecycle behavior |
| `durable-object.test.ts` | `DurableObjectRunner` HTTP-routing + suspend/resume tests | Adding DO tests, debugging routing or resume |
| `d1-storage.test.ts` | `purgeExpiredWorkflowRuns` + `purgeTenant` against real SQLite: terminal-vs-live statuses, TTL boundary, prefix-neighbor range exactness ('abc' vs 'abcdefg'), three-store offboarding, missing-table-as-empty (run-less tenant, fresh deployment), retention↔artifact pairing incl. artifacts-first crash ordering, and `purgeTenant`'s two non-atomicity pins (a run landing in the SELECT→DELETE window is reaped with its artifacts unenumerated; a failing approvals DELETE leaves the snapshots already gone) — both reachable only by self-service reset | Adding retention/offboarding tests |
| `mastra-schema-guard.test.ts` | The REAL `@mastra/cloudflare-d1` D1Store over node:sqlite: exact six-table inventory pin (enabling agent memory = a tenancy decision, not a default), `run_id` column-name guard, and the purge-vs-resume race regression | A `@mastra/core` bump, or touching purge semantics |
| `path-safe-id.test.ts` | Character-exhaustive INV-3 pins: no valid tenantId char in `[0x5F,0x60]`; tenant-salted runIds stay PATH_SAFE | Changing either id pattern |
