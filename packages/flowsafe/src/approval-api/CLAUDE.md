# approval-api/

Phase 3 approval queue: CAS-backed store, role-authorized service, REST
router, and the grant-minting seam.

## Files

| File | What | When to read |
| ---- | ---- | ------------ |
| `README.md` | Invisible knowledge: the suspend→decide→grant loop, design decisions (derivation-not-transport, suspension scoping, contract mirror, CAS-only), and the invariants bridges/providers must uphold | Before changing grant semantics, store transitions, or the breakwater contract |
| `contract.ts` | Mirrored breakwater wire contract: re-exports the requestContext key literals from `do-runner/breakwater-keys.ts` (approved-connectors, actor, workflow-scope), plus `ApprovalRole`/`ApprovalActor`, `ApprovalAuditEvent`/`ApprovalAuditSink`, and the notification seam `ApprovalNotificationEvent` ('created' \| 'escalated') / `ApprovalNotificationSink` (fire-and-forget; failures audit as `approval.notify`) | Changing the cross-package contract (must stay literal-equal to breakwater's constants — enforced by `end-to-end.test.ts`) or the notification event shape |
| `types.ts` | `ApprovalRecord` (incl. the `suspendedAt`/`resumeCount` binding pair — the exact suspension a step-keyed approval binds to; `resumedAt` retained as informational only; `runScoped`, the EXPLICIT opt-in that makes a step-less record a run-wide standing grant), statuses/priorities (`OPEN_STATUSES`, `APPROVAL_STATUSES`, `TERMINAL_APPROVAL_STATUSES`), `CreateApprovalInput`, `ApprovalListFilter` (incl. `limit` 1–500 + opaque `after` cursor + `orderBy` 'created'\|'reviewer'; triage filters `requestedBy` + strict `createdBefore`/`createdAfter` time bounds — `parseApprovalTimeBound` is the shared throw-on-garbage gate both backends and the router apply; `approvalCursor`/`parseApprovalCursor`/`MAX_APPROVAL_LIST_LIMIT`; `approvalListOrder` resolves the default and rejects the reviewer+after combination), `byReviewerOrder` — THE reviewer queue comparator (priority → SLA → FIFO) every list surface and the dashboard sort share, `ApprovalMetrics`, decide wire results (`DecideResult`, `ResumeOutcome`), batch decide (`MAX_APPROVAL_BATCH_DECIDE` = 100, `BatchDecideItem` with its HTTP-mirroring `code`, `BatchDecideResult`) | Changing the record shape, status set, metrics fields, pagination, list ordering, or decide/batch result shapes |
| `store.ts` | `ApprovalStore` contract (CAS `transition`, idempotent `create`, `list` with limit/cursor pagination — FIFO by default, reviewer order under `orderBy: 'reviewer'` applied BEFORE the limit so a bounded page is the top of the queue, SQL-aggregate-or-JS `metrics()`) + `InMemoryApprovalStore` (tenant-BOUND at construction, INV-2: required `tenantId` ctor, per-tenant predicates, create() STAMPS the tenant; optional shared Map so factory views share one backend); `stepKeyOf`, `computeApprovalMetrics`, `paginateApprovalList`, `approvalListComparator` | Changing store semantics — both stores must keep passing the shared contract suite |
| `d1-store.ts` (class NOT exported from the barrel — obtain stores via `D1ApprovalStoreFactory`) | `D1ApprovalStore`: schema DDL (partial unique open-step index, `suspended_at`/`resumed_at`/`resume_count`/`run_scoped` columns each with a defensive per-column ALTER backfill for pre-existing DBs — spike-era and earlier releases), `UPDATE ... RETURNING` CAS, lazy memoized schema creation (`#ready`/`#createSchema`), `listOrderBy` — the SQL mirror of `byReviewerOrder` both views share (store.test.ts pins the cross-backend ordering parity); `ApprovalDatabase` structural D1 subset. **Adding an `ApprovalRecord` field needs 4 explicit sites here** (DDL, ALTER list, `ApprovalRow`, INSERT+bind, `rowToRecord`) — `InMemoryApprovalStore` `structuredClone`s and round-trips it for free, so only `store.test.ts`'s all-optional-fields fixture catches a silent D1 drop | Changing the schema or SQL |
| `service.ts` | `ApprovalService` over a `TenantBoundApprovalStore`: role policy + actor-tenant≡store-binding assert, self-approval gate, claim/decide/delegate/create (runId must carry the tenant prefix — INV-1 belt)/metrics, `decideBatch` (≤`MAX_APPROVAL_BATCH_DECIDE` unique ids, sequential fan-out through the EXISTING `decide()` — per-record CAS/SoD/audit/resume untouched; per-record failures become typed `BatchDecideItem.code`s; one `approval.decide.batch` summary event; only record-independent failures throw), audit emission (tenantId in detail; the allowed-decide detail carries `durationSeconds` — queue dwell time feeding breakwater's `metricsAuditSink` histogram convention), notification firing (`notify` on `created: true` via the contained `fireNotification`; failures audit as `approval.notify`), `resumeRun` callback, typed errors; PLUS the standalone cron-only `sweepSLA(SystemApprovalStore, …)` — the one legitimate cross-tenant read+write, unreachable from request scope BY TYPE, now also firing `notify` per escalated record alongside `onEscalation` | Changing business rules, role policy, SLA behavior, batch semantics, or notification firing |
| `router.ts` | `createApprovalRouter({ resolve })`: authenticates + tenant-binds FIRST via `TenantResolver`, then routes; error→status mapping (400/401/403/404/409; `TenantResolutionError`→403); list filters incl. `requestedBy` + eagerly-400d `createdBefore`/`createdAfter`; `POST /batch/decide` (always mounted — decide fan-out, not create; matched BEFORE the generic `[id, action]` arms, where it would misparse as `decide('batch')`); `allowCreate` (default false) + the create route's TWO-layer body policy — `CLIENT_CREATE_FIELDS` allowlist pick (a field in neither list is INERT, never spread through; router.test.ts pins at the type level that the two lists exactly cover `CreateApprovalInput`) over the `TCB_ONLY_CREATE_FIELDS` 400 layer; the `/sla/sweep` route is deliberately GONE (cron-only TCB now) | Adding or changing endpoints or status codes; never widen what a body may set |
| `grants.ts` | `approvedConnectorsForLeg`, `approvalGrantProvider` (the `requestContextForRun` implementation), `defaultResumeData`, `resumeViaRuntime`; suspension binding is exact-match-first on the `(suspendedAt, resumeCount)` pair (clock-free; `resumeCount` — the runtime-owned monotonic resume ordinal, undefined on a first suspension, `1,2,…` on re-suspensions, set on every resume regardless of payload — is the collision-free categorical tie-breaker for same-ms `suspendedAt` collisions) with a legacy decidedAt-after fallback. A step-less record mints on every leg only when `runScoped === true` — never by omission | Changing how decisions become grants or resumeData |
| `tenant-brand.ts` | `TENANT_BOUND` unique-symbol brand, `TenantBoundApprovalStore` (the only store type request scope may touch), `SystemApprovalStore` (cron-only cross-tenant view; `[TENANT_BOUND]?: never` makes it unassignable in request scope; carries `purgeExpired` for retention) | Changing what "bound" means |
| `retention.ts` | `purgeExpiredApprovals(systemStore, { ttlMs, now, limit })` — cron-only terminal-status TTL purge (decidedAt with updatedAt fallback), the approval-side analog of `purgeExpiredWorkflowRuns`; open records never purged; a purged-but-still-needed APPROVED record self-heals via host-kit's reconcile-on-status (fresh approval, new decision — see header comment) | Changing approval retention or its recovery contract |
| `tenant-store.ts` | `D1ApprovalStoreFactory` / `InMemoryApprovalStoreFactory` (`forTenant()` throws on non-INV-3; one memoized DDL pass per factory; `system()` view; the in-memory factory shares ONE Map so cross-tenant tests are non-vacuous, and carries `purgeTenant(tenantId)` — the in-memory mirror of D1 purgeTenant's approvals delete, for hosts with no D1) | Obtaining stores; changing factory/schema-memo behavior |
| `tenant-context.ts` | `TenantContext`/`TenantResolver`/`createTenantResolver` — authenticate → validate (INV-3 + the reserved identity `system`, which custom verifiers bypassing `toApprovalActor` could otherwise slip through; throws `TenantResolutionError`) → bind; `service()` lazy per request, `newRunId()`/`ownsRun()`/`ownsMemoryId()` route through do-runner's shared salted-id primitives (`mintSaltedId`/`tenantOwnsSaltedId`); the agent-memory constructors `newThreadId()`/`newResourceId(key)` (same uuid seam as runIds, via do-runner memory-id); `canSelfDecide(role)` (display hint only — `DECIDER_ROLES` guard AND the `allowSelfDecision` option's `SelfDecisionPolicy`, fed the SAME value the host gives `ApprovalService` so the run-router `/workflows` echo can never contradict `decide()`'s verdict) — custom TenantContext implementations must provide all four (`newThreadId`/`newResourceId`/`ownsMemoryId`/`canSelfDecide`) | Changing the request-scoped tenant seam |
| `index.ts` | Subpackage barrel | Finding the public approval-api surface |
| `tenant-context.test.ts` | The bound context's memory-id mints: shared uuid seam with `newRunId`, resource-key salting + PATH_SAFE refusal, exact `ownsMemoryId` boundary (resolver refusal paths live in run-router.test.ts) | Changing TenantContext mints |
| `store.test.ts` | Shared store contract suite run against BOTH stores (D1 via a node:sqlite adapter), payload round-trips, SQL-injection inertness | Adding store tests |
| `service.test.ts` | Role matrix, CAS conflicts, self-approval, decide/resume outcomes, SLA boundary + sweep idempotency, metrics math, audit isolation | Adding service tests |
| `router.test.ts` | Endpoint routing + status-code mapping, auth, method/path edge cases, the create route's default-404 and its `TCB_ONLY_CREATE_FIELDS` rejection | Adding router tests |
| `grants.test.ts` | Suspension-scoped derivation (shadowing, boundary, fail-closed), explicit `runScoped` opt-in, provider output, resume wiring | Adding grant tests |
| `end-to-end.test.ts` | Cross-package proof with breakwater from source: contract tripwires, forged-resume fail-closed, two-gate and re-suspension fixtures, approve/reject loops, and the create-route capability chain (each of its three barriers pinned) | Verifying the breakwater contract or the full loop |
