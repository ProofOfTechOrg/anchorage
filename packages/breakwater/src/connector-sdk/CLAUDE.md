# connector-sdk/

## Files

| File | What | When to read |
| ---- | ---- | ------------ |
| `index.ts` | `createConnector()` wrapping `createTool()` — network-egress allowlist gate, write-approval gate (a `requestContext` grant enforced on every path), keyed idempotent replay (atomic reserve path when the store implements `AtomicIdempotencyStore`), dry-run simulation (`dryRun` manifest + `dryRunExecute`, requested via `DRY_RUN_CONTEXT_KEY`), fixed-window rate limiting (`rateLimit` manifest + `RateLimitStore`/`InMemoryRateLimitStore`), the Track B `_background` model-override defense (DL-005 — rejects a `_background` arg unless `permissions.background` opts in; `background: true` allowed only on a read-only connector, a write-class opt-in throws at construction; reach caveat: core/schema strip `_background` on the agent path, so it has teeth on no-schema/passthrough/direct calls — the `backgroundExecution` evaluator is the gate-loop counterpart), and the `ConnectorRuntime` third argument to `execute`/`dryRunExecute` (its `fetch` guard is built ONCE at construction — `egressFetch` bound to the manifest's declared egress, base injected via `policies.fetch` — and wrapped per call only to bind that call's `requestContext` for the denial audit, policy 'egress-fetch'); plus `connectorManifest`, `InMemoryIdempotencyStore` (atomic), the approved-connectors / idempotency-key / dry-run context-key constants, and `ConnectorPolicyError` | Implementing a connector, changing egress/approval/idempotency/dry-run/rate-limit/background enforcement, adding a permission-manifest field |
| `egress-fetch.ts` | `egressFetch()` — fetch-level egress enforcement: wraps a base fetch so every actual request must resolve to an allowed host (allowlist normalized ONCE at construction via `tool-policy.ts`'s `normalizeDomain`/`domainAllowed`/`assertEgressHostList` — the same lower-level matcher `egressDomainAllowed` shares) over http(s); manual redirect following with per-hop checks (fails closed on a browser's opaque status-0 redirect), cross-origin credential-header stripping, one-shot-body refusal on 307/308 (`ReadableStream` getReader OR Node-Readable/async-iterable), a validated non-negative-integer `maxRedirects`, hop cap; `EgressDeniedError` (carries `reason`), the `denied()` error-mapping seam, structural fetch types (`EgressResponse`, `EgressRequestInit`, `EgressFetchBase` — lib-ES2022-only build, no DOM types) | Changing runtime egress enforcement, redirect semantics, or the fetch seam types |
| `d1-idempotency-store.ts` | `D1IdempotencyStore` — durable `AtomicIdempotencyStore`: `INSERT ... ON CONFLICT DO NOTHING RETURNING` claim, stale-pending TTL takeover, JSON record round-trip; structural `IdempotencyDatabase`/`IdempotencyStatement` (no workers-types dependency) | Changing the durable replay store's schema, claim SQL, or TTL semantics |
| `d1-rate-limit-store.ts` | `D1RateLimitStore` — durable `RateLimitStore`: `UPSERT ... RETURNING count` atomic fixed-window increment shared across isolates (an in-memory window under DO-per-run routing is a per-RUN budget), rollover reap of expired windows; structural `RateLimitDatabase`/`RateLimitStatement` | Changing the durable budget store's schema or window semantics |
| `connector-sdk.test.ts` | Tests for every enforcement path — egress allow/deny (declaration gate AND the runtime `ConnectorRuntime.fetch` guard incl. its audit record), grant gating on nested and direct calls, idempotent replay (legacy get/put and atomic reserve paths), dry-run, rate limit | Adding enforcement tests, debugging a gate that fails open or closed |
| `egress-fetch.test.ts` | `egressFetch` unit tests — construction validation, host/wildcard/normalization matrix, scheme + unparseable-URL denials, URL-object vs Request-shaped input, redirect following (relative Locations, disallowed hops, 303 GET rewrite, 307 body preservation, one-shot stream refusal, hop cap, manual passthrough, Location-less 3xx), cross- vs same-origin credential stripping, the `denied()` seam, global-fetch default | Changing guard behavior or redirect semantics |
| `d1-idempotency-store.test.ts` | `D1IdempotencyStore` against real SQLite (node:sqlite): claim races over one database, release/retry, stale takeover, undefined-result round-trip | Adding durable-store tests, debugging the reserve CAS |
| `d1-rate-limit-store.test.ts` | `D1RateLimitStore` against real SQLite: window counting, cross-instance sharing over one database, rollover + reap, per-key isolation, schema-retry | Adding durable budget-store tests |
