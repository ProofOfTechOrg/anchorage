# policy-engine/

## Files

| File | What | When to read |
| ---- | ---- | ------------ |
| `index.ts` | `PolicyEngine` Mastra processor + agent-boundary evaluators (`denyPatterns`, `maxTextLength`, `extractMessageText`). Output is gated per channel (`OutputChannel`: answer/reasoning/object) with per-stream state accumulation, incremental `denyPatterns` scans (`PolicyContext.streamState`), and opt-in zero-leak hold-back buffering (`PolicyEngineOptions.holdBack`, per-policy `holdBackChars` hints, end-chunk/finish flush via the runner's reprocess key). Re-exports the tool-boundary evaluators from `tool-policy.ts` and the content-inspection evaluators from `content-inspection.ts`; policy/decision/phase/channel types | Adding an agent-boundary policy, gating model input/output, debugging streaming, channel, hold-back, or result gating |
| `content-inspection.ts` | `piiSecrets()` — PII/secret detectors (email, ssn, phone, creditCard with Luhn validation, awsAccessKey, privateKey header, jwt, secretAssignment, highEntropy with shape gates + Shannon threshold), allowlist exemptions (case-insensitive strings / RegExps, g/y flags stripped), denial reasons name detector id + match index only (never the matched text), incremental per-channel streaming windows sized by the enabled detectors' `maxSpan` (object channel full-rescans), `holdBackChars` hint = maxSpan − 1. `classifierPolicy()` — pluggable async classifier seam: `evaluateEveryChars` streaming cadence per channel, object snapshots and input/result phases always classify, fail-closed `timeoutMs` race (no unhandled rejection), no hold-back default (callers opt into `Infinity`). Both best-effort — evasion caveats in each JSDoc | Adding a detector, tuning entropy/allowlist semantics, wiring a moderation classifier |
| `content-inspection.test.ts` | Per-detector positive/negative matrix, Luhn check-digit cases, entropy thresholds, allowlist exemptions, reason redaction, 1-char-chunk window rescans + hold-back interplay through a real `PolicyEngine`, classifier cadence/timeout/fail-closed/cursor-isolation | Changing content-inspection behavior |
| `tool-policy.ts` | Tool-boundary evaluators (`networkEgress`, `approvalRequired`, `crossWorkflowIsolation` + `WORKFLOW_SCOPE_CONTEXT_KEY`, `tenantIsolation` + `ISOLATION_SCOPE_CONTEXT_KEY` — denies scope-less calls, dry-run included, `backgroundExecution` + `LLM_BACKGROUND_OVERRIDE_KEY` — Track B DL-005 deny-by-default for the write class when a `_background` override would enable background) with `ToolCallContext` / `ToolPolicyEvaluator` / `SideEffect` / `WritePermissionsPolicy` types, applied at connector execute; also the shared egress-match kernel: `domainAllowed` + `normalizeDomain` (exact + `*.wildcard`, precondition: already-normalized inputs) is the lower-level matcher `networkEgress` and the connector SDK's runtime `egressFetch` guard each call directly, normalizing their own allowlist once; `assertEgressHostList` is the shared host-list validator both (plus `createConnector`) call; `egressDomainAllowed` is the one-shot public wrapper — normalizes both sides then delegates to `domainAllowed` — for barrel/external callers | Enforcing egress/write/isolation policy at the tool boundary, extending the tool-policy contract |
| `policy-engine.test.ts` | `PolicyEngine` processor tests (input/output gating, tripwire, streaming, channels, hold-back buffering) | Adding processor tests, debugging gate behavior |
| `tool-policy.test.ts` | Tool-boundary evaluator tests (egress, approval, isolation decision table) | Adding tool-policy tests |
